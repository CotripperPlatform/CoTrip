import React from 'react';
import { storiesOf } from '@storybook/react';
import DiscoverGroups from './DiscoverGroups';

storiesOf('DiscoverGroups', module)
  .add("Default", () => (
    <DiscoverGroups />
  )
)
